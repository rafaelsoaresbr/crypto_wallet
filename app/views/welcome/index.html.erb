<div class="jumbotron">
  <h1 class="display-4"><%= t('welcome') %></h1>
  <p class="lead"><%= t('messages.description') %></p>
  <hr class="my-4"
  <p> <%= t('messages.options') %></p>
  <%= render "menu" %>
</div>
